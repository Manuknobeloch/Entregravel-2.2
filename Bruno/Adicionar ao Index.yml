info:
  name: Adicionar ao Index
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8080/adicionar/indice
  body:
    type: json
    data: |-
      {
        "value": "vasco",
        "index": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
