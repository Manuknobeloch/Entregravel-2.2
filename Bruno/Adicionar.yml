info:
  name: Adicionar
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8080/adicionar
  body:
    type: json
    data: |-
      {
        "value": "palmeiras"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
