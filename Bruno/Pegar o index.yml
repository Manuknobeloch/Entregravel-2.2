info:
  name: Pegar o index
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:8080/mostrar/0
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
