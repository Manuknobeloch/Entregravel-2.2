info:
  name: Reverter
  type: http
  seq: 9

http:
  method: POST
  url: http://localhost:8080/reverter
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
